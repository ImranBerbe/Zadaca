function header () {
    return (
        <div class="header">
            <nav class="Navbar">
                <a href="#">Features</a>
                <a href="#">About</a>
                <a href="#">Newsletter</a>
            </nav>
        </div>
    );
}

export default header